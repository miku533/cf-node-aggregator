name: 更新节点

on:
  # 每6小时自动运行
  schedule:
    - cron: '0 */6 * * *'
  
  # 允许手动触发
  workflow_dispatch:
  
  # 推送到 main 分支时运行
  push:
    branches:
      - main
    paths:
      - 'config.json'
      - 'src/**'
      - '.github/workflows/**'

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        
      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: 安装依赖
        run: npm install
        
      - name: 运行节点聚合
        run: npm start
        
      - name: 提交更新
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add output/
          git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 自动更新节点 $(date '+%Y-%m-%d %H:%M:%S')" && git push)